# Rust build output
/target
**/*.rs.bk

# Tool caches
/.safelint_cache/
/.ruff_cache/

# macOS
.DS_Store

# Local config & secrets — never commit real ones (examples are committed)
/arkstore.yaml
/sources.yaml
/*secrets*.yaml

# AI-client local settings (the committed skill under .claude/skills/ stays tracked)
.claude/settings.local.json
